/** "Active" pill marking the in-context item (org / project / scope) in the
 *  switcher and account menus. */
export function ActiveTag() {
  return (
    <span className="ml-auto rounded-full bg-primary/10 px-1.5 py-0.5 font-medium text-[10px] text-primary">
      Active
    </span>
  );
}
